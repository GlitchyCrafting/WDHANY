#pragma once

#include "include/sqlite_orm.h"
#include "include/hashing.h"
#include <string>

namespace gc {

  template<typename T>
  void login(std::string username, std::string password, T database) {
    
  }

  template <typename T>
  void signup(std::string username, std::string email, std::string password, T database) {
    
  }

} // namespace gc
